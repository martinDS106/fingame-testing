# Railway reads this from the repo root. Do not set a custom start command in the
# dashboard — it overrides the Dockerfile and often breaks deploys.
[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
